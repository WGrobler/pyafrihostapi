[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "afrihost-clientzone"
version = "0.0.1"
description = "Unofficial Afrihost ClientZone API"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["requests>=2.28", "beautifulsoup4>=4.11", "lxml>=4.9"]
